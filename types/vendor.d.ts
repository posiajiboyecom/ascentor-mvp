// Reserved for third-party module declarations that lack @types packages.
// Currently empty — ffmpeg/ffprobe are handled by the Trigger.dev build
// extension and accessed via process.env.FFMPEG_PATH / FFPROBE_PATH.
